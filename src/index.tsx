import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { App } from './APP/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('mileage-ui-react-root')
);
